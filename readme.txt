Installation:

1.sudo apt-get install libnet-snmp-perl
2.sudo apt-get install libdbi-perl
3.sudo apt-get install libpango1.0-dev
4.sudo apt-get install libxml2-dev
5.sudo apt-get install libsnmp-perl
6.sudo apt-get install libsnmp-dev
7.sudo apt-get install rrdtool
8.sudo perl -MCPAN -e 'install NET::SNMP'

----------------------------------------

Steps to be done:

1. The backend script can be executed in terminal using path/to/et2536-vedb15/assignment4/ and then using the command './backend.sh'

2. Wait for the values being updated in the webpage.

3. For your browser, go to index.php of assignment 4 and the details of the devices are shown in a table format.

4. Status is colour coded with 'white' represents that the system is responding and 
   the colour intensity of 'red' incidates the lost requests are in increasing order.


